### GITHUB

+ 온라인 클라우드 서버로 제공되는 GIT 저장소 서비스

+ https://github.com/Ezenitac-AiService/GITEX.git

```
# 실습할 폴더로 이동
cd GITHUB01
git clone https://github.com/Ezenitac-AiService/GITEX.git
```

+ main 브랜치 : github의 저장소 주 브랜치 이름은 'main'입니다

+ 온라인 저장소와 로컬 저장소를 복제라는 수단으로 연결
+ origin : 온라인 저장소

+ 온라인 저장소의 변경 내용을 로컬 저장소에 반영해주세요
```
git pull origin main
```

+ 현재 로컬 저장소가 어느 온라인 저장소와 연결되어있습니까?
```
git remote -v
```

+ 복제된 로컬 저장소의 파일들에 대한 '모든 권한'은 여러분에게 있습니다
+ 형상관리 가능

+ 로컬 저장소의 내용을 온라인 저장소에 반영하는것은 여러분이 '온라인 저장소에 어떤 권한이 있는가?'에 따릅니다

+ push : 로컬 저장소의 내용을 온라인 저장소에 반영해주세요
```
git push -u origin main
```

+ github 인증이 진행됩니다

+ github 인증 내용은 '자격 증명 관리자'앱에서 확인할 수 있습니다

+ ![alt text](자격증명.png)

```mermaid
flowchart TD
    %% 스타일 정의
    classDef local fill:#e3f2fd,stroke:#1565c0,stroke-width:2px,color:#0d47a1;
    classDef remote fill:#efebe9,stroke:#4e342e,stroke-width:2px,color:#3e2723;
    classDef process fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#1b5e20;
    classDef highlight fill:#fff9c4,stroke:#fbc02d,stroke-width:2px,color:#f57f17;

    subgraph LocalArea ["💻 로컬 개발 환경 (Local PC)"]
        Init["1. 저장소 초기화<br><code>git init</code>"] 
        Work["2. 파일 생성 및 수정"] 
        Add["3. 스테이징 에어리어 추가<br><code>git add .</code>"] 
        Commit["4. 로컬 커밋 생성<br><code>git commit -m '메시지'</code>"]
        
        Init --> Work --> Add --> Commit
    end

    subgraph GitHubArea ["☁️ 원격 저장소 (GitHub)"]
        CreateRepo["5. GitHub Repository 생성<br>(GitHub 웹사이트)"]
    end

    %% 연결 단계
    Commit --> Link["6. 로컬-원격 저장소 연결<br><code>git remote add origin &lt;URL&gt;</code>"]
    CreateRepo --> Link
    
    Link --> RenameBranch["7. 기본 브랜치 이름 변경<br><code>git branch -M main</code>"]
    RenameBranch --> FirstPush["8. 최초 푸시 및 기본 업스트림 설정<br><code>git push -u origin main</code>"]

    subgraph DailyWorkflow ["🔄 일상적인 작업 흐름 (반복)"]
        direction TB
        Edit["코드 수정"] --> CommitLoop["커밋 생성<br><code>git commit -am '...'</code>"]
        CommitLoop --> Pull["원격지 변경사항 동기화<br><code>git pull origin main</code>"]
        Pull --> PushLoop["원격지에 최종 푸시<br><code>git push origin main</code>"]
        PushLoop --> Edit
    end

    FirstPush --> DailyWorkflow

    %% 클래스 적용
    class Init,Work,Add,Commit local;
    class CreateRepo remote;
    class Link,RenameBranch,FirstPush highlight;
    class Edit,CommitLoop,Pull,PushLoop process;
```
